#ifndef GENERAL_HELPERS_H
#define GENERAL_HELPERS_H

#include <stdint.h>

#define TRUE (1)
#define FALSE (0)
typedef uint8_t IND_BOOL; // TODO rename???

#define MAX(a,b) (((a) > (b)) ? (a) : (b))
#define MIN(a,b) (((a) < (b)) ? (a) : (b))

#endif